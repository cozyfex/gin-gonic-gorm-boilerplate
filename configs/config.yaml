mode: dev

server:
  port: 8080

db:
  master:
    type: postgres
    host: localhost
    port: 5432
    dbName: postgres
    user: postgres
    password: postgres1234
    charSet: utf8
    sslMode: disable
    timezone: Asia/Seoul
  replicas:
    - type: postgres
      host: localhost
      port: 5432
      dbName: postgres
      user: postgres
      password: postgres1234
      charSet: utf8
      sslMode: disable
      timezone: Asia/Seoul

logging:
  level: debug
  format: text
